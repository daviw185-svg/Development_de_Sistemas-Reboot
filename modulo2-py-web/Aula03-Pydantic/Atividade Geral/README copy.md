# Iniciando FastAPI

No Git Bash

cd modulo2-python-web
cd Aula01
python -m venv venv
source venv/Scripts/activate
pip install fastapi uvicorn
uvicorn main:app --reload

Desativar o venv antes de subir o repositório:
```
deactivate
```